# ====================================
# LOCAL DATA - Never commit to git
# ====================================

# Runtime data (instance-specific)
agents/*.json
!agents/.gitkeep

tasks/*.json
!tasks/.gitkeep

resources/*.json
!resources/.gitkeep

queue/*.json
!queue/.gitkeep

humans/*.json
!humans/.gitkeep

# Sensitive data
credentials/
costs/

# Runtime logs
logs/*.log
logs/*.json
!logs/.gitkeep

# Local config (each instance has its own)
config.yaml
config.local.yaml

# Session/runtime files
*.session
*.lock
*.pid

# ====================================
# KEEP IN GIT (templates & structure)
# ====================================
# .gitkeep files preserved for directory structure
# schema/ - JSON schemas
# hooks/ - webhook handlers (code)
# integrations/ - integration templates
# checklists/ - checklist templates
# workflows/ - workflow definitions
# bookings/ - booking templates
# quotas/ - quota templates
# archived-tasks/ - task archive structure
